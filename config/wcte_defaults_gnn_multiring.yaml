defaults:
    - data: wcte_data_gnn_multiring
    - model: nonhiergat_multiring
    - tasks/train: train_gnn
    - tasks/restore_best_state: restore_best_state
    - tasks/evaluate: test
gpu_list: [0]
seed: null
dump_path: './outputs/'
hydra:
  run:
    dir: ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S.%f}
  job:
    chdir: True
